{
  "installCommand": "npm install --legacy-peer-deps --include=dev && npm rebuild rolldown",
  "buildCommand": "npm run build -w apps/web",
  "outputDirectory": "apps/web/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
